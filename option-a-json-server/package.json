{
  "name": "local-api-json-server",
  "version": "1.0.0",
  "private": true,
  "description": "Szybkie lokalne API do ćwiczeń (json-server).",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --delay 400 --routes routes.json"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}